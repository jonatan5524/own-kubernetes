kind: Pod
metadata:
  name: kube-apiserver
  namespace: apiserver
spec:
  containers:
    - name: kube-apiserver
      image: docker.io/jonatan5524/own-kubernetes:kube-api
      ports:
        - containerPort: 8080
      env:
        - name: ETCD_ENDPOINT
          value: etcd
